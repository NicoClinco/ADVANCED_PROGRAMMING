#include <iostream.h>

class data_frame()
{
 public:
  // Read from file:
  data_frame(std::string _inputfile_,std::string _configfile_);
  
   
 private:
  
};



data_frame::data_frame(std::string _inputfile_,std::string _configfile_)
{
 // Read the header row and set the titles for the
 // coloumns
}
